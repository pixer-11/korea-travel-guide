# CLAUDE.md

The repo-specific rules live in one file so every agent reads the same thing — Codex and Copilot
look for `AGENTS.md`, and duplicating it here would let the two copies drift apart.

@AGENTS.md
